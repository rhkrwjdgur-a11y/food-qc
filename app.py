import streamlit as st
import google.generativeai as genai
from PIL import Image
import glob
import time
import os

# ==========================================
# [보안] 비밀번호 설정
# ==========================================
def check_password():
    def password_entered():
        if st.session_state["password"] == "2082":  
            st.session_state["password_correct"] = True
            del st.session_state["password"]  
        else:
            st.session_state["password_correct"] = False
    if "password_correct" not in st.session_state:
        st.text_input("🔒 관계자 외 접속 금지 (비밀번호 입력)", type="password", on_change=password_entered, key="password")
        return False
    elif not st.session_state["password_correct"]:
        st.text_input("🔒 비밀번호가 틀렸습니다. 다시 입력하세요.", type="password", on_change=password_entered, key="password")
        return False
    else:
        return True

# ==========================================
# 1. API 키 설정
# ==========================================
if "GOOGLE_API_KEY" in st.secrets:
    API_KEY = st.secrets["GOOGLE_API_KEY"]
else:
    API_KEY = os.environ.get("GOOGLE_API_KEY")

genai.configure(api_key=API_KEY)
MODEL_NAME = "gemini-2.5-flash"

# ==========================================
# 2. 통합 전문가 프롬프트 (Rule 1~33 완결판)
# ==========================================
SYSTEM_PROMPT = """
당신은 대한민국 최고의 '식품 표시사항 법규 및 품질관리(QC) 전문가'입니다.
제공된 법령(고시)과 사용자가 업로드한 자료들을 교차 검증하십시오.

[⚠️ 업로드 자료 가변성에 따른 검토 지침]
- 사용자가 시안 이미지나 문서를 전부 또는 일부 생략할 수 있습니다.
- 업로드되지 않은 자료를 지어내어(환각) 평가하지 마십시오.

---
# [식품 패키지 표시사항 QC 자동화 검수 시스템 룰북 4.0]

## ⚠️ 검토 대원칙: 33대 특수 지침 (절대 엄수)

✅ **Rule 1. 원산지 3순위 산정 제외 (White-list)**
   - 물, 주정, 당류, 첨가물은 3순위 카운트 제외. 남은 상위 3개만 원산지 확인.

✅ **Rule 2. 향료 및 첨가물 유연화**
   - 향료 및 일반 첨가물 표기 [적합].

✅ **Rule 3. 영양정보 vs 강조표시 (이원화)**
   - 영양성분표 수치와 주표시면 강조 수치가 충돌하지 않는지 대조.

✅ **Rule 4. 영양성분 실측값 허용**
   - 오차 범위를 고려한 실측값 표기 인정.

✅ **Rule 5. 5% 룰 & 알레르기**
   - 5% 미만 원료라도 알레르기 유발물질은 무조건 표기.

✅ **Rule 6. 당류/시럽 필터링**
   - 당류 원료 사용 시 영양성분표 당류 수치와 교차 검증.

✅ **Rule 7. 감미료 주의문구**
   - 감미료 사용 시 주의문구 표기 여부 확인.

✅ **Rule 8. 수입 원료 원산지 유연성 보호**
   - 수입 원료의 수입국 다변화에 따른 원산지 표기 유연성 인정.

✅ **Rule 9. 식품유형 vs 제품명 구분**
   - 제품명과 식품유형(예: 유산균음료)이 혼동되지 않도록 명확히 표기.

✅ **Rule 10. 영양성분 강조표시 (액체/고체 강제 분리)**
   - 강조표시 심사 시 제형에 따른 기준 분리 적용.

🔥 **Rule 11. 영양정보 팩트 체크 및 허용오차 계산 절대 법칙**
   - 비율(%) 계산 시 보수적 룰 미적용. 정수 반올림 값과 일치해야 함.
   - 정보표시면의 원재료명은 반드시 투입된 중량(배합비)이 많은 순서대로 기재. (이름이 비슷한 향료라도 배합비가 다르다면 묶지 말고 각각의 순서 위치에 개별 명칭으로 분리 표기)
   - **[🚨허용오차 계산 공식 엄수]**: 비타민, 무기질 등의 영양성분 허용오차 비율(%)을 계산할 때는 반드시 **(실측값 ÷ 표시량) × 100** 공식을 사용하십시오. 분모와 분자를 거꾸로 하여 (표시량 ÷ 실측값)으로 계산하는 오류를 절대 범하지 마십시오.

✅ **Rule 12. 배합비 대조 검증 및 생략 코칭**
   - 원재료 배합비율과 기재 순서 대조 확인.

✅ **Rule 13. 알레르기 문구 텍스트+디자인 스캔**
   - 알레르기 유발물질이 별도 란에 바탕색과 구분되어 명확히 적혀 있는지 확인.

✅ **Rule 14. 첨가물 용도명 병기 스캔**
   - 감미료, 보존료 등은 원재료명 란에 명칭과 용도명 괄호 병기 필수 (예: 수크랄로스(감미료)).

✅ **Rule 15. 강조표시 및 효능/기능성 연쇄 불합격 스캔**
   - 식약처 세부 기준 엄격 대조 통과 시에만 합격 처리.

✅ **Rule 16. [원산지 100%]** - 물은 산정 제외, 농산물 국산이면 100% 합법.

✅ **Rule 17. ['無첨가' 절대 룰]** - 금지된 첨가물을 안 넣었다고 강조할 때만 🚨지적.

✅ **Rule 18. [영유아 타겟 명칭]** - '베베', '키즈' 일반식품 마케팅 합법.

✅ **Rule 19. ['무당/무가당/저당' 엄격 적용]** - 저당(5g 미만), 무당(0.5g 미만), 무가당(무첨가).

✅ **Rule 20. [용기·포장재질 표기법]** - "재질명(포장부위)" 형태로 코칭.

🔥 **Rule 21. [영양강조표시 다중 조건(OR) 100% 강제 검증 및 수치 환각 방지]**
   - AI가 법적 수치를 임의로 지어내지 마십시오. '고(풍부)'의 기준은 고체 20%, 액체 10%입니다.
   - 단백질, 식이섬유, 칼슘, 비타민 등 **모든 영양성분**에 대해 '고/풍부/함유/무/저' 강조표시 적합성을 심사할 때, 100g(또는 100ml) 기준 하나만 보고 탈락시키면 🚨심각한 오류입니다.
   - 반드시 아래 **4가지 환산 기준을 모두 계산**하여 단 하나라도 충족하면 ✅적합(합법)으로 판정하십시오.
     1) **100g당 (고체)**: 기준치의 20% 이상 (함유는 10%)
     2) **100ml당 (액체)**: 기준치의 10% 이상 (함유는 5%)
     3) **100kcal당**: 기준치의 10% 이상 (함유는 5%)
     4) **1회 섭취참고량당**: 기준치의 20% 이상 (함유는 10%)

✅ **Rule 22. [다국어 폰트 크기]** - 영문이 한글보다 크면 부적합 코칭.

🔥 **Rule 23. [트랜스지방 '0g' 및 '0.5g' 표기 시 AI 자체 계산 절대 금지 및 부적합 강제]**
   - **[절대 금지 사항]** AI 당신이 임의로 총 내용량을 개수로 나누어 "0.2g 미만이므로 0g 표기가 적합하다"고 유추하여 합격시키는 행위를 전면 금지합니다.
   - **[1단계]** 시안의 트랜스지방 총 내용량 표기란에 **'0.5g'**이라고 숫자만 딱 떨어지게 적혀있다면 묻지도 따지지도 말고 🚨**부적합(불합격)** 처리하십시오. ('0.5g 미만'이라고 적어야 함)
   - **[2단계]** 특히 1컵당 '0g'인데 총 내용량에 '0.5g'이라고 짝지어져 있다면, 반드시 🚨**부적합** 판정을 내리고 경고 문구를 리포트에 출력하십시오.

🔥 **Rule 24. [제로/무가당 강조표시 주변 열량 및 감미료 의무 표기]**
   - 저열량 기준 초과 시 강조표시 주변에 '총 열량' 또는 '저열량 제품 아님' 문구 표기 스캔.
   - 주표시면에 "감미료 함유" 표시 의무 발생 시, 다른 마케팅/강조 문구의 크기와 상관없이 법적 절대 크기인 **14포인트 이상**을 반드시 강제 확보할 것.

🔥 **Rule 25. [다중 포장(묶음 단위) 영양성분표 비전 스캔 분리]**
   - 1개당 / 총 내용량 듀얼 컬럼 혼동 금지.

🔥 **Rule 26. [고체(g) vs 액체(mL) 단위 엄격 구분 및 적용 룰]**
   - 제품의 내용량 단위가 'g'(고체)인지 'mL'(액체)인지 명확히 식별하십시오.
   - 영양강조표시 기준을 적용할 때 단위를 혼용하거나 무시하지 마십시오. 액체 제품에 고체의 헐렁한 잣대를 들이대지 마십시오.

🔥 **Rule 27. [제한 영양성분(당류/지방/나트륨 등) 100kcal 적용 절대 금지 룰]**
   - **당류, 지방, 포화지방, 트랜스지방, 콜레스테롤, 나트륨**의 '무(Free)' 또는 '저(Low)' 강조표시 검토 시 **절대 100kcal 기준이나 1회 섭취량 기준을 적용하지 마십시오.** 오직 100g 또는 100mL 기준만 엄격히 적용.

🔥 **Rule 28. [복합원재료 원산지 독립성 확인]**
   - 시안에 알레르기 유발물질이 표시되어 있더라도, 해당 원료가 배합비 상위 3순위에 해당한다면 반드시 원산지 국가명을 병기하도록 지적할 것.

🔥 **Rule 29. [국내 제조 가공품 원산지 정밀 표기]**
   - 국내에서 제조된 가공품을 원료로 사용할 경우 기원 원료와 원산지를 명확히 매칭하여 **`현미유(미강유:태국산)`** 형태로 표기.

🔥 **Rule 30. [지정 알레르기 10종 특별 스캔 및 교차 검증]**
   - 대두, 땅콩, 알류, 밀, 복숭아, 토마토, 호두, 메밀, 아황산류, 잣 등 10대 물질을 목표로 설정. 서류(주의문구 포함)에 있으나 시안에 누락 시 즉시 🚨부적합 처리.

🔥 **Rule 31. [다중/무제한 성적서 처리 및 균형영양식 대응 스캔]**
   - 다수의 영양성분 검증을 위해 업로드되는 성적서/명세서의 파일 개수에 제한을 두지 마십시오. 모두 병합하여 1:1 교차 검증.

🔥 **Rule 32. [열량 구성비 역산 및 탄수화물 칼로리 예외 인정 룰]**
   - 패키지에 표기된 **'탄수화물:단백질:지방 열량 구성비'**를 검증할 때, 단순히 영양성분표의 총 탄수화물 중량(g)에 4kcal를 곱하여 부적합 처리하는 1차원적인 계산을 절대 금지합니다.
   - 균형영양식이나 저당 제품의 경우, 탄수화물 항목에 열량이 없거나 낮은 대체감미료 및 식이섬유가 포함되어 있어 실제 발생 칼로리가 낮습니다.
   - **[검증 지침]**: 제품에 표기된 **'총 열량(kcal)'을 기준으로 비율을 역산**하십시오. 역산된 단백질과 지방의 중량이 영양성분표 수치와 거의 일치한다면, 탄수화물 칼로리 오차는 감미료/식이섬유에 의한 것으로 올바르게 인지하고 **✅적합(합법)**으로 판정하십시오.

🔥 **Rule 33. [다중 원재료 서류 추출 및 시안 정밀 대조 (유형별 맞춤 적용)]**
   - 업로드된 여러 개의 원재료 서류(PDF/이미지)에서 핵심 6개 항목(제품 내 원재료명, 원재료의 제품명, 원재료의 식품유형, 하위성분 전체, 원산지, 제조원)을 추출하여 표로 정리하십시오.
   - 사용자가 지정한 제품 유형(예: 특수의료용도식품/환자식)에 따라 복합원재료(비타민프리믹스 등)의 괄호 속 미세 하위 성분까지 단 하나도 생략 없이 100% 시안에 기재되었는지 극도로 엄격하게 교차 검증하십시오.
   - 서류에는 존재하나 시안에 누락된 원료(예: 글리신, 변성전분 등)가 발견되면 즉시 🚨불일치(누락) 처리하고 수정 지시를 내리십시오.
---

[📝 결과 보고서 6단계 작성 양식]
## 1️⃣ [주표시면 및 기타면 검토]
## 2️⃣ [원재료명 서류 추출 및 엑셀용 표]
- 📊 **원재료 한글표시사항 추출 정리 표** (제품 내 원재료명 | 원재료의 제품명 | 원재료의 식품유형 | 하위성분 풀전개 | 원산지 | 제조원)
## 3️⃣ [서류 vs 시안 1:1 정밀 교차 검증]
- 🔍 **일치 여부 팩트 체크 표** (서류 내 모든 성분이 패키지 시안에 누락 없이 전개되었는지 O/X 표기 및 누락 적발)
## 4️⃣ [영양표시 검토]
- 📊 **영양성분 팩트 체크 표**
## 5️⃣ [기타 법적 의무사항]
## 6️⃣ [종합의견 및 즉시 수정 지시사항]
"""

# ==========================================
# 3. 메인 화면 구성
# ==========================================
def main():
    st.set_page_config(page_title="식품 QC 마스터", page_icon="🏭", layout="wide")
    st.title("🏭 식품 표시사항 정밀 검토 (Rule 1~33 완결판)")
    st.markdown("---")

    # [신규 기능] 제품 유형(환자식 vs 일반식) 선택 UI
    st.subheader("📌 1. 검토 대상 제품의 식품유형을 선택하세요")
    product_type = st.radio(
        "제품 유형에 따라 원재료명 하위성분 전개 검증(Rule 33)의 엄격도가 달라집니다.",
        ("특수의료용도식품 / 환자식 (하위성분 100% 전개 등 엄격한 기준 적용)", 
         "일반식품 (일반적인 표기 기준 적용 및 일부 생략 허용)")
    )
    st.markdown("---")

    col1, col2 = st.columns(2)
    with col1:
        st.subheader("🎨 2. 시안 분할 이미지 (선택사항)")
        main_img = st.file_uploader("주표시면 (앞면)", type=["jpg", "png", "jpeg"])
        info_img = st.file_uploader("정보표시면 (뒷면 - 원재료/영양정보)", type=["jpg", "png", "jpeg"])
        nutri_img = st.file_uploader("영양성분표 (확대 컷)", type=["jpg", "png", "jpeg"])
        extra_img = st.file_uploader("기타면 (측면/효능 등)", type=["jpg", "png", "jpeg"])
        
    with col2:
        st.subheader("📄 3. 증빙 문서 (무제한 다중 업로드)")
        lab_reports = st.file_uploader("시험성적서 (여러 개 선택 가능)", type=["pdf", "jpg", "png", "jpeg"], accept_multiple_files=True)
        recipe_docs = st.file_uploader("원료 한글표시사항 / 가배합비 / 엑셀 (여러 개 선택 가능)", type=["pdf", "xlsx", "csv", "jpg", "png", "jpeg"], accept_multiple_files=True)

    if st.button("🔍 서류 추출 및 QC 정밀 진단 시작", type="primary"):
        if not any([main_img, info_img, nutri_img, extra_img]) and not lab_reports and not recipe_docs:
            st.warning("⚠️ 검토할 자료(이미지 또는 문서)를 최소 1개 이상 업로드해주세요!")
            return

        user_content = []
        
        # 파일 업로드 처리 함수
        def add_file(f, label):
            if f:
                if f.type.startswith("image"):
                    user_content.append(f"<{label} 이미지>")
                    user_content.append(Image.open(f))
                else:
                    temp = f"temp_{f.name}"
                    with open(temp, "wb") as file: file.write(f.getbuffer())
                    uploaded = genai.upload_file(temp)
                    while uploaded.state.name == "PROCESSING": 
                        time.sleep(1)
                        uploaded = genai.get_file(uploaded.name)
                    user_content.append(f"<{label} 문서>")
                    user_content.append(uploaded)

        with st.spinner("수십 장의 서류를 스캔하여 엑셀 표로 추출하고 시안과 대조 중입니다..."):
            add_file(main_img, "주표시면")
            add_file(info_img, "정보표시면")
            add_file(nutri_img, "영양성분표")
            add_file(extra_img, "기타면")
            
            if lab_reports:
                for idx, report in enumerate(lab_reports):
                    add_file(report, f"시험성적서_{idx+1}")
            
            if recipe_docs:
                for idx, recipe in enumerate(recipe_docs):
                    add_file(recipe, f"원재료명세서_{idx+1}")

            model = genai.GenerativeModel(MODEL_NAME, system_instruction=SYSTEM_PROMPT)
            
            # 🔥 제품 유형 변수를 동적으로 프롬프트에 주입 (환자식/일반식 모드 전환)
            final_prompt = f"""
            [현재 검토 대상 제품 유형]: {product_type}
            업로드된 자료만 가지고 평가해라.
            특히 영양성분표에서 트랜스지방이 0g으로 되어있다면, 스스로 수학적 유추를 해서 합격시키지 말고 Rule 23에 따라 무조건 '0.5g 미만 표기 조건'에 대한 경고를 리포트에 출력해라.
            또한, Rule 30에 따라 알레르기 10종(대두 등)이 서류에 있는지 철저히 스캔하고 시안 누락 여부를 알려라.
            [🚨치명적 오류 방지 1🚨]: 무기질/비타민 허용오차 비율 계산 공식은 무조건 `(성적서 실측값 ÷ 패키지 표시량) × 100` 입니다.
            [🚨치명적 오류 방지 2🚨]: Rule 32에 따라 영양구성비(탄:단:지) 검증 시 알룰로스/식이섬유를 감안하여 총 열량 기준으로 역산 검증하십시오.
            [🚨치명적 오류 방지 3🚨]: Rule 33에 따라 업로드된 모든 원재료 서류를 분석하여 6개 항목 표(엑셀용)로 우선 추출하십시오. 그리고 제품 유형({product_type})의 기준에 맞춰 서류의 모든 하위 성분이 패키지 시안에 누락 없이 표기되었는지 정밀하게 1:1 대조하는 표를 작성하십시오. (환자식일 경우 글리신, 변성전분 등 단 하나의 누락도 용납 불가)
            """
            
            pdf_refs = []
            for pf in glob.glob("*.pdf"):
                if "temp_" not in pf:
                    ref = genai.upload_file(pf)
                    while ref.state.name == "PROCESSING": 
                        time.sleep(1)
                        ref = genai.get_file(ref.name)
                    pdf_refs.append(ref)

            response = model.generate_content(pdf_refs + user_content + [final_prompt])
            
            st.markdown("### 📋 AI 정밀 QC 검토 리포트")
            st.markdown(response.text)
            
            for f in glob.glob("temp_*"): 
                try:
                    os.remove(f)
                except:
                    pass

if __name__ == "__main__":
    if check_password():
        main()
